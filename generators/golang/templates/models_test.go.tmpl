// Code generated by BeaverSpec. DO NOT EDIT

package {{ .PackageName }}

import (
    "encoding/json"
    "testing"
)

// TestModelsCompile verifies that all generated models compile correctly
func TestModelsCompile(t *testing.T) {
    // This test ensures the generated code compiles and can be instantiated
    {{ range .Models }}
    var _ {{ .Name }}
    {{ end }}
}

// TestModelsJSONSerialization verifies JSON marshaling/unmarshaling
func TestModelsJSONSerialization(t *testing.T) {
    {{ range .Models }}
    {{- if and (not .IsAnyOf) (not .IsOneOf) }}
    t.Run("{{ .Name }}", func(t *testing.T) {
        original := {{ .Name }}{}
        data, err := json.Marshal(original)
        if err != nil {
            t.Fatalf("failed to marshal {{ .Name }}: %v", err)
        }
        
        var decoded {{ .Name }}
        if err := json.Unmarshal(data, &decoded); err != nil {
            t.Fatalf("failed to unmarshal {{ .Name }}: %v", err)
        }
    })
    {{- end }}
    {{ end }}
}
