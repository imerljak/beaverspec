// Code generated by BeaverSpec. DO NOT EDIT

package {{ .PackageName }}

{{- if .Imports }}
import (
{{- range .Imports }}
    "{{ . }}"
{{- end }}
)
{{- end }}

{{- if .Enums }}
// Enum type definitions
{{- range .Enums }}

// {{ .TypeName }} {{ .Description }}
type {{ .TypeName }} {{ .BaseType }}

const (
{{- $enumType := .TypeName }}
{{- range .Values }}
    {{ .Name }} {{ $enumType }} = "{{ .Value }}"
{{- end }}
)
{{- end }}
{{- end }}

{{ range .Models }}
{{- if .IsArray }}
// {{ .Name }} {{ .Description }}
type {{ .Name }} []{{ .ArrayType }}
{{- else if .IsMap }}
// {{ .Name }} {{ .Description }}
type {{ .Name }} map[string]{{ .MapValueType }}
{{- else if .IsOneOf }}
// {{ .Name }} {{ .Description }} (oneOf union type)
type {{ .Name }} struct {
{{- range .UnionVariants }}
    {{ . }} *{{ . }} `json:"-"`
{{- end }}
}

// UnmarshalJSON implements custom JSON unmarshalling for {{ .Name }}
func (u *{{ .Name }}) UnmarshalJSON(data []byte) error {
    // Extract discriminator field
    var disc struct {
        {{ .Discriminator.PropertyName | toPascalCase }} string `json:"{{ .Discriminator.PropertyName }}"`
    }
    if err := json.Unmarshal(data, &disc); err != nil {
        return fmt.Errorf("failed to unmarshal discriminator for {{ .Name }}: %w", err)
    }

    // Switch on discriminator value
    switch disc.{{ .Discriminator.PropertyName | toPascalCase }} {
    {{- range $key, $value := .Discriminator.Mapping }}
    case "{{ $key }}":
        var variant {{ $value }}
        if err := json.Unmarshal(data, &variant); err != nil {
            return fmt.Errorf("failed to unmarshal {{ $value }}: %w", err)
        }
        u.{{ $value }} = &variant
        return nil
    {{- end }}
    default:
        return fmt.Errorf("unknown discriminator value for {{ .Name }}: %s", disc.{{ .Discriminator.PropertyName | toPascalCase }})
    }
}

// MarshalJSON implements custom JSON marshaling for {{ .Name }}
func (u {{ .Name }}) MarshalJSON() ([]byte, error) {
{{- range .UnionVariants }}
    if u.{{ . }} != nil {
        return json.Marshal(u.{{ . }})
    }
{{- end }}
    return []byte("null"), nil
}

{{- else if .IsAnyOf }}
// {{ .Name }} {{ .Description }} (anyOf union type - represented as interface{})
type {{ .Name }} interface{}
{{- else }}
// {{ .Name }} {{ .Description }}
type {{ .Name }} struct {
{{- range .EmbeddedTypes }}
    {{ . }}
{{- end }}
{{- range .Properties }}
    {{ .Name | toPascalCase }} {{ .Type }} `json:"{{ .JsonTag }}"`{{- if or .Description .Default .ReadOnly .WriteOnly .Deprecated }} //{{- if .Description }} {{ .Description }}.{{- end }}{{- if .Default }} Default: {{ .Default }}.{{- end }}{{- if .ReadOnly }} Read-only.{{- end }}{{- if .WriteOnly }} Write-only.{{- end }}{{- if .Deprecated }} DEPRECATED.{{- end }}{{- end }}
{{- end }}

}

{{- if .HasValidation }}
// Validate returns an error if any field violates its constraints.
func (m *{{ .Name }}) Validate() error {
{{- range .Properties }}
{{- if .HasValidation }}
    // {{ .Name | toPascalCase }}
{{- if .Required }}
    if m.{{ .Name | toPascalCase }} {{ zeroCheck .Type }} {
        return fmt.Errorf("{{ .Name }}: required field is missing")
    }
{{- end }}
{{- if .MinLength }}
    if len(m.{{ .Name | toPascalCase }}) < {{ deref .MinLength }} {
        return fmt.Errorf("{{ .Name }}: length must be at least {{ deref .MinLength }}")
    }
{{- end }}
{{- if .MaxLength }}
    if len(m.{{ .Name | toPascalCase }}) > {{ deref .MaxLength }} {
        return fmt.Errorf("{{ .Name }}: length must be at most {{ deref .MaxLength }}")
    }
{{- end }}
{{- if .Minimum }}
    if float64(m.{{ .Name | toPascalCase }}) < {{ derefF .Minimum }} {
        return fmt.Errorf("{{ .Name }}: value must be >= {{ derefF .Minimum }}")
    }
{{- end }}
{{- if .Maximum }}
    if float64(m.{{ .Name | toPascalCase }}) > {{ derefF .Maximum }} {
        return fmt.Errorf("{{ .Name }}: value must be <= {{ derefF .Maximum }}")
    }
{{- end }}
{{- if .Pattern }}
    if matched, _ := regexp.MatchString(`{{ .Pattern }}`, m.{{ .Name | toPascalCase }}); !matched {
        return fmt.Errorf("{{ .Name }}: value does not match required pattern")
    }
{{- end }}
{{- if .HasEnum }}
    switch m.{{ .Name | toPascalCase }} {
    case {{ range $i, $v := .EnumValues }}{{ if $i }}, {{ end }}"{{ $v }}"{{ end }}:
        // valid
    default:
        return fmt.Errorf("{{ .Name }}: value %v is not a valid enum value", m.{{ .Name | toPascalCase }})
    }
{{- end }}
{{- end }}
{{- end }}
    return nil
}
{{- end }}

{{- end }}
{{ end }}

// Validator is an interface implemented by models that have validation rules.
type Validator interface {
    Validate() error
}

// APIError is a standard interface for generated API Error structs mapping to status codes
type APIError interface {
    error
    StatusCode() int
}